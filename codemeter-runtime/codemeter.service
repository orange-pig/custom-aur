[Unit]
Description=CodeMeter RunTime Server
After=network.target

[Service]
User=codemeter
ExecStart=/usr/bin/fakeroot /usr/bin/CodeMeterLin -f
ExecReload=/bin/kill -USR1 $MAINPID
Restart=always

[Install]
WantedBy=multi-user.target
